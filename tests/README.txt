It is *strongly* suggested that the tests in this directory are run with:
```
pytest -W error
```
as the examples are selected such that warning should only arrise when pytest is ready to catch them, and if they don't, something has gone wrong.
